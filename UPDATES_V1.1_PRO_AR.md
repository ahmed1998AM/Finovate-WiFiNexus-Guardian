# WiFiNexus Guardian - تحديثات الإصدار 1.1.0 Pro

## 🚀 إضافات جوهرية جديدة (v1.1.0 Pro)

تم إضافة 3 مكونات رئيسية لرفع مستوى البرنامج من أداة سلبية إلى منصة اختبار اختراق نشطة واحترافية:

---

### 1. 🔥 محرك الهجوم النشط (Active Attack Engine)
**الملف:** `network/active_attack_engine.py` (259 سطر)

#### الميزات الجديدة:
- **هجوم Deauthentication**: إرسال حزم إلغاء مصادقة مستهدفة أو عامة
  - دعم استهداف عميل محدد (`-c client_mac`) لتقليل الضجيج
  - وضع أمان يحدد عدد الحزم تلقائياً لمنع الكشف
- **التقاط PMKID**: استخراج PMKID مباشرة من نقطة الوصول
  - لا يحتاج لعميل متصل (أكثر هدوءاً وفعالية)
  - تكامل مع `hcxdumptool` و `hcxpcapngtool`
  - إنتاج ملف جاهز لـ Hashcat (format 16800)
- **هجوم MDK4**: استخدام أداة MDK4 لهجمات أكثر قوة
  - دعم قوائم سوداء لاستهداف شبكات محددة
  - تحكم في المدة الزمنية
- **هجوم دوري ذكي**: نمط "اضرب واهرب"
  - هجوم قصير ثم انتظار لالتقاط الهاند شيك
  - يقلل من بصمة الشبكة ويزيد التخفي

#### الأمان:
- حدود افتراضية لعدد الحزم (10 حزم كحد أقصى في الوضع الآمن)
- إيقاف طارئ لجميع العمليات عند المقاطعة
- تسجيل شامل للهجمات للتقارير اللاحقة

---

### 2. 🖥️ الواجهة التفاعلية النصية (Interactive TUI)
**الملف:** `core/interactive_tui.py` (257 سطر)

#### الميزات:
- **واجهة بصرية كاملة** باستخدام مكتبة `curses`
- **قائمة رئيسية تفاعلية**:
  1. مسح الشبكات
  2. إدارة الواجهات
  3. التقاط الهاند شيك
  4. هجوم نشط (جديد!)
  5. كسر كلمات المرور
  6. إدارة الأدوات
  7. الخروج
- **عرض جدولي للشبكات**: BSSID, SSID, Channel, Signal, Security
- **تنقل بالأسهم** وتحديد بالـ Enter
- **شريط حالة سفلي** يعرض الرسائل الفورية
- **ألوان مخصصة** للعناوين، التحديد، والتحذيرات

#### طريقة التشغيل:
```bash
python core/interactive_tui.py
```

---

### 3. 🧹 نظام تنظيف العمليات والحماية (Process Cleanup & Safety)
**الملف:** `core/process_cleanup.py` (139 سطر)

#### المشكلة التي يحلها:
عند تشغيل هجمات أو التقاط، تبقى عمليات خلفية (`aireplay-ng`, `airodump-ng`, etc.) عالقة حتى بعد إغلاق البرنامج، مما يسبب:
- استهلاك موارد
- تعارض في المستقبل
- كشف النظام كأداة اختراق

#### الحل:
- **تسجيل تلقائي** لكل عملية فرعية يتم تشغيلها
- **معالجة إشارات النظام** (SIGINT, SIGTERM) لإغلاق آمن عند Ctrl+C
- **تنظيف شامل عند الخروج**:
  1. قتل جميع العمليات المسجلة
  2. إعادة واجهات المراقبة لوضع الإدارة العادي
  3. قتل أي عمليات عالقة بأسمائها المعروفة
- **استخدام `atexit`** لضمان التنفيذ حتى في حالات الخطأ غير المتوقعة

#### التكامل:
يمكن دمجه في أي سكربت كالتالي:
```python
from core.process_cleanup import ProcessCleanupManager

cleaner = ProcessCleanupManager()
proc = subprocess.Popen(['aireplay-ng', ...])
cleaner.register_process(proc)
# عند الخروج سيتم تنظيف كل شيء تلقائياً
```

---

## 📊 الإحصائيات المحدثة:

| المكون | العدد |
|--------|-------|
| **ملفات Python** | **43 ملف** (+3) |
| **أسطر برمجية جديدة** | **~655 سطر** |
| **إجمالي الكود** | **~9,155 سطر** |
| **وحدات أساسية** | **11 وحدة** |

---

## 🛠️ كيفية الاستخدام المتقدم:

### 1. هجوم Deauth مستهدف:
```python
from network.active_attack_engine import ActiveAttackEngine

engine = ActiveAttackEngine("wlan0mon", safety_mode=True)
engine.send_deauth(
    target_bssid="AA:BB:CC:DD:EE:FF",
    client_mac="11:22:33:44:55:66",  # عميل محدد
    count=10,
    duration=5
)
```

### 2. التقاط PMKID:
```python
success = engine.capture_pmkid(
    target_bssid="AA:BB:CC:DD:EE:FF",
    output_file="/tmp/pmkid_capture",
    timeout=30
)
# الملف الناتج: /tmp/pmkid_capture.16800 (جاهز لـ Hashcat)
```

### 3. تشغيل الواجهة التفاعلية:
```bash
python core/interactive_tui.py
```

### 4. دمج نظام التنظيف:
```python
from core.process_cleanup import ProcessCleanupManager
import subprocess

cleaner = ProcessCleanupManager()

# تشغيل عملية
proc = subprocess.Popen(['airodump-ng', 'wlan0mon'])
cleaner.register_process(proc)

# عند الضغط على Ctrl+C أو انتهاء البرنامج:
# سيتم إيقاف airodump-ng وإعادة wlan0mon لوضعه الطبيعي تلقائياً
```

---

## ⚠️ تحذيرات أمنية وقانونية مُعززة:

1. **الهجمات النشطة (Deauth/PMKID)**:
   - قد تُعتبر جريمة إلكترونية في بعض الدول إذا لم تكن مصرحاً بها
   - تسبب انقطاع خدمة للعملاء الشرعيين (DoS)
   - **استخدم فقط في بيئة معزولة ومصرح بها تماماً**

2. **وضع الأمان (Safety Mode)**:
   - مفعل افتراضياً ويحد من عدد الحزم والمدة
   - يمكن تعطيله في البيئة المعزولة فقط (`safety_mode=False`)
   - لا نوصي بتعطيله أبداً في الشبكات الإنتاجية

3. **التنظيف التلقائي**:
   - ضروري جداً لتجنب ترك آثار في النظام
   - يحميك من نسيان عمليات تعمل في الخلفية
   - يقلل من فرص الكشف بواسطة أدوات EDR

---

## 🎯 خارطة الطريق المستقبلية (v1.2.0+):

- [ ] تكامل فعلي للـ TUI مع الوحدات الخلفية (حالياً محاكاة)
- [ ] دعم Multi-threading في الواجهة لعدم التجميد
- [ ] تقارير PDF/HTML احترافية
- [ ] قاعدة بيانات SQLite لسجل الهجمات والشبكات
- [ ] تكامل WiGLE API لرفع البيانات
- [ ] حاوية Docker جاهزة للاستخدام

---

**المطور:** Ahmed Mostafa Ibrahim (Finovate – AHMED EG)  
**الإصدار:** 1.1.0 Pro © 2025  
**البلد:** مصر 🇪🇬

**البرنامج الآن يمتلك العضلات والعقل والعيون لأداء اختراق حقيقي!** 💪🧠👁️

---

## 📊 الإحصائيات النهائية المحدثة:

- **43 ملف Python** (زيادة +3 ملفات جديدة)
- **8,448 سطر برمجي** في الوحدات الرئيسية
- **13 ملف توثيق Markdown** شاملة
- **11 وحدة أساسية** متكاملة

### الملفات الجديدة في هذا الإصدار:
1. `network/active_attack_engine.py` (259 سطر) - محرك الهجوم النشط
2. `core/interactive_tui.py` (257 سطر) - الواجهة التفاعلية
3. `core/process_cleanup.py` (139 سطر) - نظام التنظيف والحماية
4. `UPDATES_V1.1_PRO_AR.md` - توثيق التحديثات
